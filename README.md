# enodia-test — временный тестовый репозиторий

Здесь лежит **черновой снимок** канала обновления проекта Enodia: ровно те файлы, которые
роутер скачивает сам (`update-manifest.txt` + `bin/` + `SHA256SUMS`). Репозиторий заведён,
чтобы прогнать обновление, не трогая боевой, и **будет удалён**.

Ставить отсюда ничего не нужно и не следует. Проект живёт в другом месте.

---

# enodia-test — temporary test repository

A scratch snapshot of the Enodia update channel — only the files a router downloads by itself.
Created to exercise the updater without touching the real repository, and **will be deleted**.

Do not install from here.
